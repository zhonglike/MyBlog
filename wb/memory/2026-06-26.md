# 2026-06-26 工作日志

## MyBlog 私人博客系统搭建完成
- 技术栈：Node.js Express 5 + better-sqlite3 + 原生HTML/CSS/JS前端
- 后端：server.js + db.js + 5个路由模块（auth/posts/media/categories/settings）+ auth中间件
- 前端：login.html + index.html(SPA) + main.css + 三主题CSS(dark/light/cyber) + 4个JS模块
- 数据库：SQLite 6表（users/categories/posts/media/settings/view_history）
- 功能验证：注册/登录/创建帖子/视频帖子/分类/推荐全部API测试通过
- Express 5 修复：`*` 通配符路由改为 middleware 方式
- 问卷确认的用户需求：单用户、本地上传+外链视频、四项个性化（主题/分类/布局/推荐）、局域网访问、Node.js Express、SQLite存储、账号密码登录、暗色+亮色+赛博朋克三主题
- 用户要求以后做任务先反问需求（交互式问卷）

## MyBlog 新增功能
- 二级密码：注册时可选设置(最短2位)，敏感操作(编辑/删除/设置/新建)时弹窗验证，错3次踢回登录页
- 记住登录：JWT有效期改为30天
- 设置页面4大板块：账户信息(昵称/改密码/改二级密码)、个性化外观(博客标题/副标题/三主题/布局拖拽排序)、内容偏好(默认分类/每页数量/排序方式)、数据备份(导出zip含数据库+文件)
- 后端新增：auth路由加secondary_password/profile/verify-secondary接口，settings路由加export接口，db.js加migration函数，users表加secondary_password_hash/avatar字段
- 安装archiver用于备份导出打包

## MyBlog 新增：个人主页 + 外网访问指南
- 个人主页四大板块：资料卡(头像/昵称/签名/注册时间/发帖数/浏览量)、数据统计面板(发帖数/浏览量/图片数/视频数/分类分布/最热帖子Top5)、最新博文展示、媒体画廊(图片+视频缩略图瀑布流)
- 导航栏头像按钮入口，点击进入个人主页
- 后端新增 /api/auth/profile-stats GET接口，返回完整用户资料+统计数据+分类分布+最热帖子+最近博文+媒体画廊
- auth.js updateProfile 加 bio(个性签名)字段，db.js migration加bio和display_name字段
- 外网访问方案：公网IP+端口映射（用户选择），写了完整指南（E:\myblog\外网访问指南.md）含申请公网IP、路由器配置、防火墙放行、域名绑定、Cloudflare Tunnel替代方案

## Bug修复：个人主页profile-stats接口
- posts表缺user_id和category字段 → db.js加字段定义 + migration函数加ALTER TABLE
- createPost/updatePost需写入user_id(req.user.id)和category名称 → posts.js修复INSERT/UPDATE语句
- media表字段是original_name不是originalname → auth.js SQL查询修复m.originalname→m.original_name
- 全部12项API测试通过：注册/登录/资料修改/bio/个人主页统计/分类/帖子CRUD/二级密码验证/设置

## Cloudflare Tunnel 外网访问配置完成
- 下载 cloudflared.exe v2026.6.1 到 E:\myblog\cloudflared.exe（51.7MB）
- npm全局也安装了cloudflared wrapper v0.7.1（C:\Users\zxqh\AppData\Roaming\npm\cloudflared.cmd）
- 用 Quick Tunnel（免费无需账号）启动：`E:\myblog\cloudflared.exe tunnel --url http://localhost:3000`
- 当前隧道地址：https://gamma-unlikely-paint-cad.trycloudflare.com
- 全部连通性检查 PASS（DNS/UDP/TCP/API）
- 外网访问验证：返回 200 ✅
- 用户真实公网IP：223.102.73.172（辽宁大连中国移动），移动不给公网IP所以端口映射方案不可行
- VPN残留代理设置（HTTP_PROXY=http://127.0.0.1:10090）影响网络，需清除后才能查真实IP
- Quick Tunnel是临时地址，每次启动会换域名；如需固定域名需注册Cloudflare账号+创建named tunnel

## 局域网IP和外网域名提示优化
- server.js 启动提示局域网地址从 `<你的IP>` 改为固定 `192.168.1.39:3000`
- 新建 start-tunnel.js：Node.js脚本启动博客+Tunnel，自动从cloudflared输出提取临时域名并高亮显示
- blogtunnel.bat 改为调用 node start-tunnel.js（不再用笨拙的BAT解析）
- 外网访问指南.md 重写：强调移动无公网IP、Quick Tunnel每次域名变化、三种固定域名方案
- F:\WRO备份失败（F盘当前未挂载）
