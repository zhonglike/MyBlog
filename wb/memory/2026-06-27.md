# 2026-06-27 工作日志

## 视频推荐系统重构 + Bug修复
- Bug修复：关闭详情弹窗时iframe(video src)不清除导致视频后台继续播放 → closeDetail()改为先清除所有iframe.src和video.pause()+video.src=''再隐藏弹窗
- 新增video_pool数据库表（title/video_url/source/tags/group_tag/cover_image）
- 预设6条三角洲行动B站视频链接到池中
- 新建video-pool.js路由：GET /api/video-pool/recommend(每日随机)、GET /api/video-pool/groups、POST+DELETE管理接口
- 每日随机算法：用当天日期字符串生成偏移量，同一天看到相同顺序
- 首页推荐区重构：1大卡(主推)+4小卡布局，素材来自视频池+用户视频帖子混排
- 新增三角洲专区section（delta-section），展示三角洲行动全部视频
- 推荐卡点击弹窗播放(playRecommendVideo函数)，本地帖子点击showDetail
- CSS新增：recommend-layout(1大+4小flex布局)、rec-main-card/rec-small-card、delta-section/delta-card全套样式
- 响应式适配：768px以下推荐区转为单列
- 布局排序新增三角洲选项，默认顺序改为pinned/recommend/delta/latest/videos/categories
- 设置页layout-sort-list新增三角洲行
- server.js注册video-pool路由
- API测试全部通过
